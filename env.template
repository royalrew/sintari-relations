# Public (kan visas i klient)
NEXT_PUBLIC_APP_NAME=Sintari Relations
NEXT_PUBLIC_APP_VERSION=v0.1
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Server (för produktion)
NODE_ENV=production

# PDF/Chromium (valfritt, används lokalt)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome

# Stripe (för betalningar)
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID=price_...

# Autentisering
PASSWORD_SALT=byt-mig-till-en-säker-salt-sträng

# Valfritt för framtida OpenAI-integration
# OPENAI_API_KEY=sk-...

# PR7: Brain-First Core Feature Flags
# Memory V2: Default OFF in production, enable via canary or explicit flag
MEMORY_V2=0
MEMORY_V2_CANARY=0
MEMORY_PATH=data/memory_v2
MEMORY_K_EPISODIC=6
MEMORY_K_SEMANTIC=8
MEMORY_W_SEM=0.6
MEMORY_W_EPI=0.4
MEMORY_RECENCY_BOOST=0.35
MEMORY_HALFLIFE_DAYS=7
MEMORY_TTL_DAYS=90
MEMORY_LRU_MAX_ITEMS=5000

PERSONA_V1=on
EMOTION_GATES=on

# PR9: Canary Rollout (percentage of traffic)
CANARY_PERCENT=5

# Telemetry paths
WORLDCLASS_LIVE_PATH=reports/worldclass_live.jsonl

# PR10: Production Budget & Rate Limits
RUN_LIMIT_USD=0.05
WEEKLY_LIMIT_USD=150.0
RATE_LIMIT_BURST=10
RATE_LIMIT_WINDOW=10

# Admin Dashboard (för test execution)
ADMIN_SECRET=byt-mig-till-en-säker-nyckel

# Style & Chat Feature Flags
NEXT_PUBLIC_SHOW_INTERJECTION=false
NEXT_PUBLIC_SHOW_THINK=false
NEXT_PUBLIC_SHOW_RECEPTION_DEMO=1
NEXT_PUBLIC_SHOW_QUALITY_GATES=1
NEXT_PUBLIC_SHOW_LAUNCH_GUARDRAILS=1
NEXT_PUBLIC_SHOW_PRICING_CTA=1

# Telemetry Sampling (base:1,post3:3 = 1:1 first 3 turns, then 1:3)
STYLE_TELEMETRY_SAMPLE=base:1,post3:3

# Canary (auto-backoff controlled by drift alarm)
# Keep at 5% until strict-lang-match PASS and parity_p95_like_gap ≤ 0.02 (24h)

