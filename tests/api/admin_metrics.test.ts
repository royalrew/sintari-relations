import { describe, it, expect } from "@jest/globals";

describe("GET /api/admin/metrics", () => {
  it("returns agg + thresholds + status", async () => {
    expect(true).toBe(true);
  });
});

